<% flash.each do |type, msg| %>
  <% if type != 'error'%>
    <div class="alert alert-success">
      <%= msg %>
    </div>
  <% else %>
    <div class="alert alert-danger">
      <%= msg %>
    </div>
  <% end %>
<% end %>